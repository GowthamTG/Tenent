// import { FormControl, FormGroupDirective, NgForm } from '@angular/forms';

// export class CrossFieldErrorMatcher  {
//   isErrorState(
//     control: FormControl,
//     form: FormGroupDirective | NgForm
//   ): boolean {
//     return (
//       (control.dirty && form.hasError('alreadyExists')) ||
//       (control.invalid && control.dirty)
//     );
//   }
// }
// && form.invalid
